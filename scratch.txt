'''
def find_quick_suggestion(root, prefix: str, word_length):
	lengthCount = 0
	suggestions = {}
	strBuild = []
	node = root
	for char in prefix:
		char_not_found = True
		for child in node.children:
			if child.char == char:
				char_not_found = False
				node = child
				break
	for child in node.children:
		




    return suggestion

	if not root.children:
		return False, 0

	return
'''

